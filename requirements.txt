# Core dependencies for role_boundary_info_transmission.ipynb.
# On Google Colab, torch + matplotlib are preinstalled; only the first two are pip-installed by the
# notebook. This file is for local/non-Colab reproduction.
#
# For a CPU-only or non-CUDA machine, install the appropriate torch build first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformer_lens>=2.0
transformers>=4.42
torch>=2.4
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.26
